# Architecture Problems

| Severity | Location | Reason | Recommendation | Estimated Difficulty |
| --- | --- | --- | --- | --- |
| High | `app/Http/Controllers/ItemController.php:24-425`, `app/Http/Controllers/InvoiceController.php:111-498`, `app/Http/Controllers/SalaryController.php:67-589`, `app/Http/Controllers/WorkLogController.php:11-295`, `app/Http/Controllers/ManagementReportController.php:27-587` | These controllers contain business rules, calculations, cross-module orchestration, and direct database work instead of delegating to services and repositories. This violates the documented flow. | Split business workflows into focused services/actions, move query logic into repositories, and keep controllers to request/authorize/validate/dispatch/response. | High |
| High | `app/Repositories/AccountingDocumentRepository.php`, `app/Repositories/FinancialReportRepository.php`, `app/Repositories/TreasuryRepository.php` versus the rest of `app/Http/Controllers/*` and `app/Livewire/*` | The repository layer exists, but it is not used consistently. Most modules still query models directly from controllers and Livewire components. | Introduce repositories per module or per aggregate and migrate read/write queries out of controllers and Livewire. | High |
| High | `app/Services/FinancialReportService.php:30-1767`, `app/Services/NewPayrollEngineService.php:1-924`, `app/Services/AccountingPostingService.php:1-712`, `app/Services/FiscalPeriodService.php:1-609` | Several services have become god classes with too many responsibilities and too many branches to be safely changed. | Split by sub-domain or report family, keep orchestration in the parent service, and move repeated query logic into repositories/helpers. | High |
| Medium | `app/Livewire/Employees/Form.php:103-178`, `app/Livewire/Employees/Index.php:42-88`, `app/Livewire/Payroll/Periods.php:52-130`, `app/Livewire/WorkLogs/Manager.php:42-57`, `app/Livewire/Items/Index.php:55-108` | Livewire components are performing persistence, cross-entity updates, and domain decisions. The UI layer is doing service work. | Move state-changing behavior into services/actions and let Livewire call those services only. | Medium |

