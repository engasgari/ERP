<div class="py-4 py-md-5"><div class="bg-white rounded-lg shadow-md p-3 p-md-4 space-y-4">
    <h2 class="text-xl font-bold mb-0">قراردادها</h2>
    @include('livewire.partials.flash')
    <div class="erp-ui-filter-bar"><div class="row g-2"><label class="erp-filter-field col-12 col-md-9">جستجو<input wire:model.live.debounce.400ms="search"></label><div class="col-12 col-md-3 d-grid"><button type="button" wire:click="clearFilters" class="erp-action-btn">حذف فیلتر</button></div></div></div>
    @if($editingId)<form wire:submit.prevent="saveBody" class="erp-ui-filter-bar"><label class="erp-filter-field">متن قرارداد<textarea wire:model="body" rows="8"></textarea></label><div class="d-grid d-sm-flex gap-2 mt-2"><button class="erp-action-btn erp-action-edit">ذخیره متن</button><button type="button" wire:click="$set('editingId', null)" class="erp-action-btn">انصراف</button></div></form>@endif
    <div class="table-responsive"><table class="erp-ui-data-table w-full"><thead><tr><th>شماره</th><th>پرسنل</th><th>نوع</th><th>شروع</th><th>پایان</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>@forelse($contracts as $contract)<tr wire:key="contract-{{ $contract->id }}"><td>{{ $contract->number }}</td><td>{{ $contract->employee->full_name }}</td><td>{{ $contract->contract_type }}</td><td>{{ formatJalaliDateSafe($contract->start_date) }}</td><td>{{ formatJalaliDateSafe($contract->end_date, '-') }}</td><td>{{ $contract->status === 'issued' ? 'صادر شده' : 'پیش‌نویس' }}</td><td><div class="d-grid d-sm-flex gap-2"><button wire:click="editBody({{ $contract->id }})" class="erp-action-btn erp-action-edit">ویرایش متن</button><button wire:click="issue({{ $contract->id }})" class="erp-action-btn erp-action-detail">صدور</button></div></td></tr>@empty<tr><td colspan="7" class="text-center py-6 text-slate-500">قراردادی ثبت نشده است. تایید حکم کارگزینی قرارداد پیشنهادی می‌سازد.</td></tr>@endforelse</tbody></table></div>
    {{ $contracts->links() }}
</div></div>
